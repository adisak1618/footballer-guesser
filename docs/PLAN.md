# Implementation Plan: Headball MVP

Generated by /plan-eng-review on 2026-04-30
Reviewed against design doc: `~/.gstack/projects/board-game/adisakchaiyakul-main-design-20260430-001244.md`

## Architecture

```
┌─────────────────┐         ┌──────────────────┐
│  Mobile Browser │ ◄─────► │  Vercel (Next.js)│
│  (1 per player) │  HTTPS  │  - App Router    │
└─────────────────┘         │  - RSC + Client  │
                            └────────┬─────────┘
                                     │
                                     │ supabase-js
                                     ▼
                            ┌──────────────────┐
                            │     Supabase     │
                            │  - Postgres      │
                            │  - Realtime      │
                            │  - RLS (anon key)│
                            └──────────────────┘
```

## Tech Stack (locked)

- Next.js 16 (App Router) + TypeScript strict mode
- Supabase (Postgres + Realtime + RLS, no Auth)
- Zustand for client state
- Zod for input validation
- Tailwind + shadcn/ui (mobile-first)
- Vitest (unit) + Playwright (E2E)
- Vercel deployment

## Database Schema

```sql
-- Rooms
create type room_status as enum ('LOBBY', 'PLAYING', 'ENDED');

create table rooms (
  id uuid primary key default gen_random_uuid(),
  code char(6) unique not null,
  status room_status default 'LOBBY',
  current_round int default 0,
  max_rounds int not null,
  score_positions int not null,
  category text not null default 'premier-league',
  host_player_id uuid,
  created_at timestamptz default now()
);

-- Players (per room)
create table players (
  id uuid primary key default gen_random_uuid(),
  room_id uuid references rooms(id) on delete cascade,
  player_id uuid not null, -- localStorage UUID
  display_name varchar(20) not null,
  join_order int not null,
  connected bool default true,
  total_score int default 0,
  unique(room_id, player_id)
);

-- Round state per player
create table round_state (
  id uuid primary key default gen_random_uuid(),
  room_id uuid references rooms(id) on delete cascade,
  round_number int not null,
  player_id uuid not null,
  assigned_name text not null,
  score_this_round int default 0,
  is_active bool default true,
  final_position int, -- null until guessed
  unique(room_id, round_number, player_id)
);

-- Atomic position counter per round
create table round_positions (
  room_id uuid references rooms(id) on delete cascade,
  round_number int not null,
  next_position int default 1,
  primary key (room_id, round_number)
);

-- Round events (audit log)
create type event_type as enum ('GUESS_OK', 'FOUL', 'ROUND_END');

create table round_events (
  id bigserial primary key,
  room_id uuid references rooms(id) on delete cascade,
  round_number int not null,
  player_id uuid not null,
  type event_type not null,
  guess_text text,
  position int,
  created_at timestamptz default now()
);

-- Football players (seed data, 100 ชื่อ Premier League MVP)
create table football_players (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  category text not null,
  index(category)
);

-- RLS: anyone with anon key can read all rooms (lookup by code)
-- writes go through Postgres functions (atomic operations)
```

## Atomic Guess Function

```sql
create or replace function submit_guess(
  p_room_id uuid,
  p_round_number int,
  p_player_id uuid,
  p_guess text
) returns table (correct bool, position int, score int)
language plpgsql as $$
declare
  v_assigned text;
  v_pos int;
  v_score_pos int;
  v_score int;
  v_correct bool;
begin
  -- Lock the player's row
  select assigned_name into v_assigned
  from round_state
  where room_id = p_room_id
    and round_number = p_round_number
    and player_id = p_player_id
    and is_active = true
  for update;

  if v_assigned is null then
    raise exception 'Player not active or not found';
  end if;

  -- Fuzzy match (lower + trim)
  v_correct := lower(trim(p_guess)) = lower(trim(v_assigned));

  if v_correct then
    -- Atomic position increment
    update round_positions
    set next_position = next_position + 1
    where room_id = p_room_id and round_number = p_round_number
    returning next_position - 1 into v_pos;

    select score_positions into v_score_pos from rooms where id = p_room_id;

    if v_pos <= v_score_pos then
      v_score := v_score_pos - v_pos + 1;
    else
      v_score := 0;
    end if;

    update round_state
    set score_this_round = v_score,
        is_active = false,
        final_position = v_pos
    where room_id = p_room_id
      and round_number = p_round_number
      and player_id = p_player_id;

    insert into round_events (room_id, round_number, player_id, type, guess_text, position)
    values (p_room_id, p_round_number, p_player_id, 'GUESS_OK', p_guess, v_pos);
  else
    -- Foul
    update round_state
    set is_active = false
    where room_id = p_room_id
      and round_number = p_round_number
      and player_id = p_player_id;

    insert into round_events (room_id, round_number, player_id, type, guess_text)
    values (p_room_id, p_round_number, p_player_id, 'FOUL', p_guess);
  end if;

  return query select v_correct, v_pos, coalesce(v_score, 0);
end;
$$;
```

## File Structure

```
board-game/
├── app/
│   ├── page.tsx                # Landing (Create + Join CTA)
│   ├── join/page.tsx           # Join with code form
│   └── room/[code]/
│       ├── page.tsx            # Lobby + Game container (client)
│       ├── lobby.tsx           # Component: pre-start
│       ├── playing.tsx         # Component: active round
│       └── results.tsx         # Component: end of game
├── components/
│   ├── ui/                     # shadcn/ui primitives
│   ├── name-card.tsx           # Big name card (face-out display)
│   ├── guess-modal.tsx         # Modal for guess input
│   └── connection-status.tsx   # 🔴 Reconnecting banner
├── lib/
│   ├── supabase.ts             # Client setup
│   ├── room-code.ts            # 6-char code gen + collision retry
│   ├── game-store.ts           # Zustand store
│   ├── schemas.ts              # Zod validators
│   └── types.ts                # Re-export Supabase generated types
├── data/
│   └── premier-league.ts       # 100 player names seed
├── supabase/
│   ├── migrations/
│   │   ├── 0001_init.sql
│   │   └── 0002_seed_players.sql
│   └── config.toml
├── e2e/
│   ├── lobby.spec.ts
│   ├── full-game.spec.ts
│   ├── race-guess.spec.ts      # CRITICAL — concurrent guess
│   ├── foul.spec.ts
│   └── reconnect.spec.ts
├── lib/__tests__/
│   ├── room-code.test.ts
│   ├── game-store.test.ts
│   └── scoring.test.ts
└── PLAN.md (this file)
```

## Implementation Steps (10 tasks, ~1 weekend with Claude Code)

### Lane A — Backend setup (independent, can parallel with Lane B)
1. **Supabase project init** — local Docker dev + create project on supabase.com
2. **Schema migration 0001_init.sql** — all tables + RLS policies + submit_guess function
3. **Seed data 0002** — 100 Premier League players (curated from Wikipedia)

### Lane B — Frontend scaffolding (independent, parallel with Lane A)
4. **Next.js app setup** — App Router + Tailwind + shadcn/ui init
5. **Zustand store + types** — game-store.ts with all state + Supabase generated types

### Lane C — Game logic (depends on Lane A schema + Lane B store)
6. **Room create/join flow** — `app/page.tsx`, `app/join/page.tsx`, `app/room/[code]/page.tsx` lobby
7. **Round engine** — start round (random order + names), guess flow (calls submit_guess), round end detection via Realtime subscription
8. **Score display + next round** — accumulate total_score, max_rounds check

### Lane D — Polish (depends on C)
9. **Connection banner + reconnect** — localStorage player_id resume flow
10. **Tests** — Vitest unit + Playwright E2E (5 specs)

### Worktree parallelization

| Step | Modules | Depends on |
|------|---------|------------|
| 1, 2, 3 | supabase/ | — |
| 4, 5 | app/, lib/ | — |
| 6, 7, 8 | app/, lib/ | 1, 2, 5 |
| 9 | components/, lib/ | 7 |
| 10 | __tests__/, e2e/ | 6, 7, 8, 9 |

**Lane A (1,2,3) + Lane B (4,5)** = parallel worktrees. Then converge for Lane C.

## What's NOT in scope

- ~~**Multiple categories** — defer until 1 validates~~ (Phase 2 unblocked: lobby settings UI now writes `rooms.category`. Add new categories by seeding `football_players` + extending `update_room_settings` allowlist.)
- **User accounts / Auth** — anonymous only, no login
- **QR code for room join** — defer (typing 6 chars OK for MVP)
- **Voice/video chat** — players are same-room, talk in person
- **Timer per turn** — players manage themselves (per founder decision)
- **Stale room cleanup job** — defer (TODO)
- **Player photos / club logos** — license risk (TODO if licensed)
- **Push notifications** — none in MVP
- **Analytics dashboard** — defer (use Vercel Analytics free tier passively)
- **Custom domain** — vercel.app subdomain for MVP
- **i18n** — Thai only at launch
- **Custom decks (user upload names)** — defer to validate first

## What already exists

- Nothing — greenfield. Only `game-rules.md` describing game logic.

## Failure modes (per new codepath)

| Codepath | Failure mode | Test? | Error handled? | User sees clear error? |
|----------|-------------|-------|----------------|-----------------------|
| `createRoom()` | code collision (rare) | ✅ unit (mock collision) | ✅ retry up to 5x | n/a (silent retry) |
| `joinRoom()` | room not found | ✅ E2E | ✅ 404 page | ✅ "ห้องไม่พบ" |
| `joinRoom()` | room full (>8) | ✅ E2E | ✅ reject | ✅ "ห้องเต็ม" |
| `joinRoom()` | room PLAYING | ✅ E2E | ✅ reject | ✅ "เกมเริ่มแล้ว" |
| `submit_guess()` | concurrent guess race | ✅ E2E | ✅ Postgres sequence | n/a (atomic) |
| `submit_guess()` | network drop mid-submit | ⚠️ manual test | ✅ retry button | ✅ "ลองอีกครั้ง" |
| Realtime subscription | Supabase disconnect | ✅ unit | ✅ banner + auto-reconnect | ✅ 🔴 banner |
| `reconnect()` | localStorage lost | ⚠️ edge | ✅ rejoin as new player | ✅ confirm dialog |

**Critical gaps**: 0 — ทุก failure mode มี test หรือ obvious user feedback

## UI Design Specifications

Generated by /plan-design-review on 2026-04-30. All decisions calibrate against `DESIGN.md` (Stadium Energy).

### Screen Layouts (4 screens)

#### Screen 1: Landing (`/`)

```
┌─────────────────────────┐ 100dvh, bg: --ink (#0a0e1a)
│                         │
│       HEADBALL          │ ← Anton 56px (display-xl), --on-dark
│                         │
│ ทายชื่อนักฟุตบอลบนหัว     │ ← IBM Plex Thai 16px (body-md), --on-dark-soft
│                         │
│   (subtle floodlight    │ ← decorative gradient at top edge only
│    glow at top)         │
│                         │
│  ┌───────────────────┐  │
│  │ +  สร้างห้อง       │  │ ← button-goal, 56px, --goal bg
│  └───────────────────┘  │
│                         │
│  ┌───────────────────┐  │
│  │   เข้าห้อง           │  │ ← button-secondary, 44px
│  └───────────────────┘  │
│                         │
│   วิธีเล่น               │ ← text-link, on-dark-muted (footer)
└─────────────────────────┘
```

#### Screen 2: Lobby (`/room/[code]`)

```
┌─────────────────────────┐ bg: --ink
│ ← ออกจากห้อง            │ ← caption + back arrow, on-dark-muted (top-left)
│                         │
│  ROOM CODE              │ ← caption-uppercase
│ ┌─────────────────────┐ │
│ │  W  F  T  9  K  M   │ │ ← room-code-display, Bebas 48px, letter-spacing 8px
│ └─────────────────────┘ │
│  📋 แตะเพื่อ copy        │ ← caption, on-dark-soft
│                         │
│  GAME SETTINGS          │ ← host-only editor (display-md, surface-elevated)
│  ┌─────────────────┐    │
│  │ Rounds   [−][5][+]   │ ← stepper 1-20, host edits, guest reads
│  │ Top-N    [−][3][+]   │ ← stepper 1..player_count-1, clamped on player drop
│  │ Category [Premier ▾] │ ← dropdown, locked once any round played
│  │ ┌──────────────────┐ │
│  │ │  Save settings   │ │ ← goal-colored CTA, only when dirty
│  │ └──────────────────┘ │
│  └─────────────────┘    │
│                         │
│  PLAYERS (4/8)          │ ← display-md (Anton 28px)
│  ┌─────────────────┐    │
│  │ 🔴  อ้อ          │    │ ← player-chip, --tag-red bg, --on-dark text
│  ├─────────────────┤    │
│  │ 🔵  บอย         │    │ ← --tag-blue
│  ├─────────────────┤    │
│  │ 🟡  ปาน          │    │ ← --tag-yellow
│  ├─────────────────┤    │
│  │ 🟢  คุณ          │    │ ← --tag-green, "you" indicator
│  └─────────────────┘    │
│                         │
│  เกมจะเริ่มเมื่อทุกคนพร้อม │ ← body-sm, on-dark-soft
│                         │
│  ┌───────────────────┐  │
│  │  ⚽ START GAME     │  │ ← button-goal (host only — non-host sees "รอ host เริ่มเกม")
│  └───────────────────┘  │
└─────────────────────────┘
```

#### Screen 3: Playing — BIG NAME card (HERO)

**Default state — face-out display:**

```
┌─────────────────────────┐ Full viewport (100dvh)
│                         │ bg: --tag-{playerColor}
│  ROUND 2/5              │ ← caption-uppercase, top-left fixed
│                         │
│                         │
│      STEVEN             │ ← Bebas Neue 144px (hero-name)
│      GERRARD            │ ← centered, all-caps, --on-dark
│                         │
│                         │
│   (subtle stadium grain texture)
│                         │
│  ─── tap to act ────    │ ← caption, on-dark-soft (bottom hint)
└─────────────────────────┘
```

**On tap — control overlay (covers name immediately, honor system):**

```
┌─────────────────────────┐ overlay: rgba(0,0,0,0.95)
│  ✕  cancel              │ ← top-right, returns to display in 100ms
│                         │
│  YOUR TURN              │ ← display-md (Anton 28px)
│  เลือก action            │
│                         │
│  ┌───────────────────┐  │
│  │  ⚽ ทายชื่อ          │  │ ← button-goal (red)
│  └───────────────────┘  │
│                         │
│  ┌───────────────────┐  │
│  │  🏆 ดูคะแนน          │  │ ← button-secondary
│  └───────────────────┘  │
│                         │
│  ⚠️ ทายผิด = Foul          │ ← caption, --warning
│      (ออกจากรอบ)         │
│                         │
│  ⏱ 10s → return         │ ← auto-timeout countdown, caption
└─────────────────────────┘
```

**On guess — Modal:**

```
┌─────────────────────────┐
│  ทายชื่อนักเตะของคุณ     │ ← title-lg
│                         │
│  ┌─────────────────────┐│
│  │ Steven Gerrard      ││ ← text-input (focused border --goal)
│  └─────────────────────┘│
│                         │
│  ⚠️ ทายผิด = Foul,        │ ← caption, --warning
│     ออกจากรอบทันที       │
│                         │
│  [ยกเลิก]  [ส่งคำตอบ]    │ ← secondary + goal buttons
└─────────────────────────┘
```

#### Screen 4: Results

```
┌─────────────────────────┐ bg: --canvas (#fafbfc), light-mode for celebration
│                         │
│      FINAL SCORE        │ ← display-xl (Anton 56px), --on-light
│                         │
│ ┌─────────────────────┐ │
│ │  1   อ้อ        12   │ │ ← scoreboard-tile, Bebas 32px, tabular-nums
│ ├─────────────────────┤ │
│ │  2   บอย       8     │ │
│ ├─────────────────────┤ │
│ │  3   ปาน        5    │ │
│ ├─────────────────────┤ │
│ │  4   คุณ        3    │ │
│ └─────────────────────┘ │
│                         │
│   🎉 อ้อ ชนะ!           │ ← display-md + confetti animation
│                         │
│  ┌───────────────────┐  │
│  │  เล่นรอบใหม่         │  │ ← button-goal
│  └───────────────────┘  │
│                         │
│  ┌───────────────────┐  │
│  │  ออกจากห้อง         │  │ ← button-secondary
│  └───────────────────┘  │
└─────────────────────────┘
```

### Interaction State Coverage

| FEATURE | LOADING | EMPTY | ERROR | SUCCESS | PARTIAL |
|---------|---------|-------|-------|---------|---------|
| Create room | Spinner on button → "กำลังสร้าง..." | n/a | "สร้างห้องไม่สำเร็จ ลองใหม่" + retry | Navigate to lobby + room code | n/a |
| Join via code | Skeleton on player list 200ms | "ยังไม่มีผู้เล่นอื่น" + share code prompt | "ห้องไม่พบ" / "ห้องเต็ม" / "เกมเริ่มแล้ว" friendly Thai + back button | Player added to list with tag color | n/a |
| Lobby waiting | n/a | (host alone) "รอเพื่อนกดเข้าห้อง — แชร์รหัส W F T 9 K M" | Connection drop → 🔴 reconnecting banner top | All 4-8 players with chips | 2/8 → grey "รอเพื่อน..." |
| Round start | Countdown "3... 2... 1..." 600ms each | n/a | n/a | BIG NAME card slides in 600ms ease-bounce + haptic | n/a |
| Submit guess | Inline spinner 200ms | n/a | "เครือข่ายขาด ลองอีกครั้ง" + retry | Green flash + haptic + score flip | n/a |
| Foul state | n/a | n/a | n/a (it IS the error) | Red shake + grey out card + "FOUL" overlay 600ms | n/a |
| Round end | n/a | "รอผู้เล่นคนอื่น..." while some still active | n/a | Slide to scoreboard 300ms | n/a |
| Reconnect | 🔴 "กำลังเชื่อมต่อ..." pulse banner | n/a | After 30s: "เชื่อมต่อไม่ได้ — กลับหน้าหลัก?" | Banner slides up | n/a |

**Edge case visuals:**
- Display name input: char counter `12/20`, validation ≥1 char ≤20
- Player count 8 max: at 7/8 "รอผู้เล่นอีก 1 คน", at 8/8 lock + "ห้องเต็ม"
- Network drop during countdown: pause countdown, show banner, resume on reconnect
- Player drops mid-round: their chip greys in lobby + score, game continues for others

### User Journey Storyboard

| Step | User does | User feels | Plan supports |
|------|-----------|------------|---------------|
| 1 | เปิด link → landing | "ไหนดู?" curious | Stadium glow + big "Headball" — instantly understood |
| 2 | กด "เข้าห้อง" + พิมพ์รหัส 6 ตัว | "อย่าพิมพ์ผิด" cautious | 6-char input + tap-to-paste from clipboard |
| 3 | พิมพ์ชื่อ + เห็น list | "เพื่อนมาแล้ว?" anticipation | Player list updates live via Realtime, tag colors |
| 4 | Host กด START | "เริ่มแล้ว!" excitement | Countdown 3-2-1 + GOAL letter cascade |
| 5 | เห็น BIG NAME card | "อ๋าโดนไรบนหัว?" confusion + funny | Bebas 144px in jersey color, readable from 1.5m |
| 6 | มอง phone เพื่อน + ถามคำถาม | สนุก + คุยกับเพื่อน | App ไม่ขัดจังหวะ — peer-managed (per founder decision) |
| 7 | กดจอ → control overlay | "เรามั่นใจหรือยัง?" hesitation | Cover overlay + "ทายผิด = Foul" warning |
| 8 | พิมพ์ guess → submit | "ผ่านได้มั้ย..." anticipation | Loading 200ms → reveal |
| 9a | ถูก → green flash + haptic + score flip | 🎉 ชนะ! เหมือนยิงประตู | Stadium energy delivered |
| 9b | ผิด → red shake + Foul | 😩 fail แต่ฮา | Grey card + "FOUL" overlay |
| 9c | เห็น GuessResult (Correct/Foul) | "ได้กี่คะแนน? ชื่ออะไรกันแน่?" reveal | Transient full-bleed result screen — Anton 88px headline, Bebas 96px score, assigned-name reveal, total-score pill (foul). Auto-advances after 8s; tap to skip; instant cut on realtime round-advance |
| 10 | ดู scoreboard ระหว่างรอบ | "ใครนำ?" comparison | Bebas tabular-nums, ranking |
| 11 | จบเกม → final scoreboard | 🏆 หรือ 😅 | Confetti + winner name + replay button |
| 12 | กด "เล่นรอบใหม่" | "อีกตา!" addicted | Same room, fresh names, instant restart |

**Time-horizon design:**
- **5-sec visceral**: Stadium dark + jersey colors + Bebas 144px = "ฟุตบอล!" instantly
- **5-min behavioral**: Foul mechanic + scoring = พฤติกรรม "คิดก่อนทาย"
- **5-year reflective**: "เคยเล่นกับเพื่อน Champions League final 2026"

### Responsive & Accessibility

#### Breakpoints (mobile-first)
- **320-374px** (small phone): hero-name scales 144px → 96px, all other tokens unchanged
- **375-480px** (standard, primary): all DESIGN.md tokens default
- **480-768px** (large phone/small tablet): max-width 480px centered, --ink extends to fill viewport

#### Touch Targets (WCAG AAA mobile)
- All buttons min 44px tall
- Goal button (primary CTA): 56px
- Player chip: 56px tall (room to tap to view profile)
- Room code: 48px tall
- Tap-to-cover: full screen during BIG NAME display

#### Color Contrast (verified WCAG AA min)
- on-dark on --ink: 19.5:1 ✅ AAA
- on-dark on --tag-yellow: 4.6:1 ✅ AA (large text only — name 144px qualifies)
- on-dark on --tag-blue: 7.2:1 ✅ AAA
- on-dark on all other tags: ≥4.5:1
- --goal on --ink: 4.8:1 ✅ AA

#### A11y Specs
- BIG NAME card: `role="img"` `aria-label="Your assigned name is hidden — show this screen to other players to play"`
- Player chips: `aria-label="Player {name}, color {color}, score {score}"`
- Score updates: `aria-live="polite"` on scoreboard
- Toasts: `aria-live="assertive"` on 🔴 reconnecting banner
- Tab order: room code → join → display name → submit
- Enter submits, Esc closes modal
- `prefers-reduced-motion: reduce` → disable confetti, foul shake, GOAL cascade (keep functional motion + haptic)

#### iOS Safari quirks
- Use `100dvh` not `100vh` (avoid bottom-bar issue)
- `<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">` (prevent zoom on input)
- Disable user-select on game UI
- Disable double-tap zoom on game screens
- Wake Lock API: prevent screen sleep during gameplay (request on round start)

### Cover/Uncover Mechanic (DECISION LOCKED)

**Mechanism: Tap = control overlay + honor system**

When player taps anywhere on BIG NAME card:
1. Dark overlay (rgba(0,0,0,0.95)) covers name immediately (100ms fade)
2. Control buttons appear ("ทายชื่อ", "ดูคะแนน", cancel ✕)
3. After 10s of inactivity OR cancel tap → returns to BIG NAME display
4. After "ทายชื่อ" tap → modal opens (overlay stays, name still hidden)
5. After submit → result animation → returns to display (or marked inactive if foul)

**Why honor system not DeviceOrientation:**
- Real Heads Up game uses honor system — proven UX
- DeviceOrientation API requires permission on iOS Safari (extra friction)
- Honor system = 0 implementation risk, players self-police (peer pressure)
- If a player peeks, they get foul on wrong guess — natural punishment

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 0 | — | (covered by /office-hours) |
| Codex Review | `/codex review` | Independent 2nd opinion | 0 | — | skipped (efficiency) |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | CLEAR | 5 issues, 0 critical gaps |
| Design Review | `/plan-design-review` | UI/UX gaps | 1 | CLEAR | score: 5→9/10, 1 decision (cover mechanic) |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | n/a (consumer app) |

**UNRESOLVED:** 0
**VERDICT:** ENG + DESIGN CLEARED — ready to implement

