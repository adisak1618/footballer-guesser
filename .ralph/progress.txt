# Ralph Loop Progress — Headball Multi-Game Platform

Branch: feat/multigame-platform
Total stories: 83
Started: 2026-05-08

## Codebase Patterns

- Root `package.json` uses Bun workspaces (`apps/*`, `packages/*`). `apps/` and `packages/` dirs do not need to exist for `bun install` to succeed — empty globs are tolerated.
- Existing Headball app code lives at repo root through Phase 0; Phase 1 relocates it to `apps/headball/`.
- Turborepo v2 requires `packageManager` field in root `package.json` to resolve workspaces (currently `bun@1.1.29`).
- Turbo v2 schema uses `tasks` (not `pipeline`) — see `turbo.json`. Build outputs include `.next/**` (excluding cache) and `dist/**` for libraries.
- Bun 1.1.29 generates `bun.lockb` (binary), not `bun.lock` — turbo emits a harmless WARNING about parsing the lockfile but still functions; upgrading to Bun ≥1.1.42 (text lockfile) would silence it.
- Pre-existing tech debt: root `bun run lint` reports thousands of errors (eslint scope issue / type-aware rules in deps) — predates Phase 0 work, not a regression. Phase 1 relocation will likely scope eslint to `apps/headball/` and reduce the surface.
- Workspace packages under `packages/*` declare `"main": "./src/index.ts"`, `"types": "./src/index.ts"`, and `"exports": { ".": "./src/index.ts" }` so consumers import TS source directly (no build step) — matches D3 in the design doc.
- `next lint` is removed in Next 16 — do NOT add `"lint": "next lint"` to per-app `package.json`. Use `eslint` directly when wiring per-app lint (Phase 0.4/0.7).
- `.turbo/` (per-package turbo cache + logs) is gitignored at the repo root — covers every workspace package without repeating in each.
- Shared TS config lives at `tsconfig.base.json` (root). Apps/packages extend via `"extends": "../../tsconfig.base.json"`. Base owns `target/module/strict/jsx/baseUrl/paths`; consumers add only Next plugin + `include`. Don't redeclare `paths` per-package.
- `@social-hub/*` path alias resolves to `packages/*/src/index.ts` (set in base). Consume with `import { X } from "@social-hub/core"` — no deep imports (matches D3).
- Root `tsconfig.json` (existing Headball app) does NOT extend base by design — it stays Next-13-style for the legacy root app until Phase 1 relocates Headball into `apps/headball/` and that app gets its own base-extending tsconfig.
- `bunx tsc --noEmit` from a sub-dir (e.g. `cd apps/hub && bunx tsc --noEmit`) picks up that workspace's tsconfig — useful for verifying per-package extends work.

## Story Completion Log

## 2026-05-08 — US-001 (Phase 0.1 — Initialize Bun workspaces)
- Added `"workspaces": ["apps/*", "packages/*"]` to root `package.json`.
- Verified: `bun install` clean, `bunx tsc --noEmit` clean, `bun run dev` returns HTTP 200 on http://localhost:3000.
- Files changed: `package.json`.
- **Learnings:**
  - Bun 1.1.29 accepts `workspaces` glob patterns even when no matching directories exist yet — useful for incremental scaffolding.
  - Next.js 16.2.4 starts in ~200ms with Turbopack; quick to spawn for smoke checks.
---

## 2026-05-08 — US-002 (Phase 0.2 — Add Turborepo with dev/build/lint/test pipelines)
- Installed `turbo@2.9.10` as workspace-root devDependency via `bun add -d turbo`.
- Added `packageManager: "bun@1.1.29"` to root `package.json` (turbo v2 hard-requires this to discover workspaces).
- Created `turbo.json` with v2 `tasks` schema:
  - `dev` — `cache: false`, `persistent: true`
  - `build` — `dependsOn: ["^build"]`, `outputs: [".next/**", "!.next/cache/**", "dist/**"]`
  - `lint` — `dependsOn: ["^build"]`
  - `test` — `dependsOn: ["^build"]`, `outputs: ["coverage/**"]`
- Verified: `bunx turbo run lint` exits 0 (0 packages in scope, expected — `apps/`/`packages/` still empty); `bunx vitest run` 57/57 pass; `bunx tsc --noEmit` clean.
- Confirmed `bun run lint` was already failing on parent commit `8bdf3a1` via stash diff — pre-existing, not a regression.
- Files changed: `package.json`, `turbo.json`, `bun.lockb`.
- **Learnings:**
  - First `bunx turbo run` against this repo errored with `Could not resolve workspaces -> Missing packageManager field`. Fixing that requires only adding `packageManager` to root `package.json`; turbo doesn't otherwise validate the version.
  - Turbo prints a non-fatal WARNING about `bun.lock` not being found because Bun 1.1.29 still uses the binary `bun.lockb` format. Functional behavior is unaffected.
  - `outputs` in `turbo.json` should explicitly exclude `.next/cache/**` to avoid shipping stale cache artifacts up to remote cache once that is enabled.
---

## 2026-05-08 — US-003 (Phase 0.3 — Empty apps/ and packages/ scaffolding)
- Created apps/{hub,insider} as minimal Next 16 placeholder apps (page.tsx + layout.tsx + next.config.ts + tsconfig.json + package.json).
- apps/headball is empty with `.gitkeep` only — Phase 1 relocates current root code here.
- Created packages/{core,ui,types,content} each with `package.json` (name `@social-hub/<name>`, version `0.0.1`, `main`/`types`/`exports` -> `./src/index.ts`) and `src/index.ts` containing `export {};`.
- Added `.turbo/` to `.gitignore` so per-package turbo cache logs aren't tracked.
- Removed `next lint` script from placeholder apps — Next 16 dropped that command; Phase 0.4/0.7 will wire eslint properly. Apps with no code have nothing to lint anyway.
- Verified: `bun install` resolves all 6 workspace packages (`ls node_modules/@social-hub/` shows `content core hub insider types ui`); `bunx tsc --noEmit` clean; `bunx vitest run` 57/57 pass; `bunx turbo run lint` discovers all 6 packages in scope (zero tasks executed since no lint scripts yet); `bun run dev` still serves HTTP 200 on `localhost:3000` from root Headball.
- Files changed: `.gitignore`; `apps/{hub,insider}/{package.json,next.config.ts,tsconfig.json,app/page.tsx,app/layout.tsx}`; `apps/headball/.gitkeep`; `packages/{core,ui,types,content}/{package.json,src/index.ts}`; `bun.lockb`.
- **Learnings:**
  - `next lint` command was removed in Next 16 — `next dev/build/start` work as usual but `lint` errors with `Invalid project directory provided`. Use `eslint` directly (or `next` v15-style script via separate eslint config) when wiring real lint per app in later phases.
  - Bun workspaces resolve via symlinks under `node_modules/@social-hub/<name>` — quick way to confirm workspace discovery without running anything.
  - Each workspace package needs `main`/`types`/`exports` pointing at `./src/index.ts` so consuming TS imports resolve to the source file in dev (matches design doc D3 — no build step for shared packages).
  - Turbo's per-package `.turbo/turbo-<task>.log` files MUST be gitignored — adding to root `.gitignore` once handles all packages.
  - When there's a placeholder app whose `package.json` lacks a script, `bunx turbo run <task>` shows "0 successful, 0 total" with a `WARNING  No tasks were executed` — that's the expected "nothing to do" state, not a failure.
---

## 2026-05-08 — US-004 (Phase 0.4 — tsconfig.base.json with strict mode and path aliases)
- Created `tsconfig.base.json` at workspace root: `target: ES2022`, `module: esnext`, `moduleResolution: bundler`, `strict: true`, `jsx: preserve`, `baseUrl: "."`, `paths: { "@social-hub/*": ["packages/*/src/index.ts"] }`. Also includes `lib`, `esModuleInterop`, `resolveJsonModule`, `isolatedModules`, `skipLibCheck`, `noEmit`, `incremental`, `allowJs`.
- Updated `apps/{hub,insider}/tsconfig.json` to extend `../../tsconfig.base.json` and only retain `plugins: [{ name: "next" }]` + `include`/`exclude`. Stripped redundant compilerOptions inherited from base.
- Created `packages/{core,ui,types,content}/tsconfig.json` extending base with `include: ["src/**/*.ts", "src/**/*.tsx"]`.
- Did NOT touch the root `tsconfig.json` — it stays Next-13-style for the legacy Headball app at root until Phase 1 relocates that code.
- Verified: `bunx tsc --noEmit` from workspace root passes; `bunx tsc --noEmit` inside each apps/* and packages/* passes; placed a temp probe file `apps/hub/path-probe.ts` importing all four `@social-hub/*` packages and confirmed it type-checked clean (then removed).
- Files changed: `tsconfig.base.json` (new); `apps/hub/tsconfig.json`, `apps/insider/tsconfig.json` (rewritten to extend base); `packages/{core,ui,types,content}/tsconfig.json` (new).
- **Learnings:**
  - In TS 4.1+ `paths` declared in an extended config are resolved relative to the base config's directory — so putting `baseUrl: "."` + `paths` in `tsconfig.base.json` at root means a consumer at `apps/hub/tsconfig.json` resolves `@social-hub/core` to `<root>/packages/core/src/index.ts` without re-declaring anything.
  - `include` and `exclude` are NOT inherited via `extends`; each consumer must declare its own. Keep them per-package.
  - `jsx` belongs in base (set to `preserve` for Next apps). React-only library packages don't render JSX in src yet, so `preserve` doesn't hurt them; if a package later needs `react-jsx` for tests, override locally.
  - The wildcard `paths` mapping `@social-hub/*` → `packages/*/src/index.ts` only resolves bare-package imports (`@social-hub/core`), not deep imports (`@social-hub/core/foo`). That matches D3's "barrel-export only" rule.
---

## 2026-05-08 — US-005 (Phase 0.5 — Create three Vercel projects with rootDirectory mapping) — PAUSE: manual work required

This story requires Vercel dashboard work that the autonomous agent cannot perform. The Ralph loop is pausing on US-005 until the maintainer completes the steps below and manually flips `passes: true` in `.ralph/prd.json`.

### What the maintainer must do

**1. Create three Vercel projects** (https://vercel.com/new → "Import Git Repository" → pick this repo, then repeat for each):

| Project name | rootDirectory | Framework Preset | Install Command | Build Command | Output Directory |
|---|---|---|---|---|---|
| `social-hub-hub` | `apps/hub` | Next.js | `cd ../.. && bun install` | `cd ../.. && bunx turbo run build --filter=@social-hub/hub` | `apps/hub/.next` |
| `social-hub-headball` | `apps/headball` | Next.js | `cd ../.. && bun install` | `cd ../.. && bunx turbo run build --filter=@social-hub/headball` | `apps/headball/.next` |
| `social-hub-insider` | `apps/insider` | Next.js | `cd ../.. && bun install` | `cd ../.. && bunx turbo run build --filter=@social-hub/insider` | `apps/insider/.next` |

For each project:
- Set "Production Branch" to `main`. Preview deploys will fire from `feat/multigame-platform` automatically.
- Node.js version: 22.x (or default 24.x — both fine).
- Disable "Automatically expose System Environment Variables" if you prefer cleaner env scoping (optional).

**Note on `social-hub-headball`:** the directory is currently empty (only `.gitkeep`) — the build will likely succeed with "No app to build" or fail soft. That's fine for Phase 0; Phase 1 (US-008+) populates it. If it errors, set the build command to `echo "headball not yet relocated, see Phase 1"` for now and revert once Phase 1 lands.

**2. Add shared env vars to each of the three projects** (Project Settings → Environment Variables):

The values come from the staging Supabase project (US-006). If US-006 is not yet done, **complete US-006 first** — that story creates the staging project and gives you the URL and anon key. Once you have them:

| Variable | Value | Environments |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | `https://<staging-ref>.supabase.co` | Production, Preview, Development |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | (anon key from staging Supabase Settings → API) | Production, Preview, Development |

Tip: `vercel env add NEXT_PUBLIC_SUPABASE_URL preview` from the CLI is faster than the dashboard once each project is linked locally with `vercel link`.

**3. Trigger preview deploys**

Push the current `feat/multigame-platform` branch (already pushed at HEAD `045dcd6`). Each Vercel project will fire a preview deploy automatically. Wait for all three to go green.

Expected results:
- **hub preview** → renders "Hub placeholder" text (from `apps/hub/app/page.tsx`).
- **insider preview** → renders "Insider placeholder" text (from `apps/insider/app/page.tsx`).
- **headball preview** → builds (empty app for now; may show 404 on `/` since `apps/headball` has no pages yet — that's expected for Phase 0).

**4. Document preview URLs**

Paste the three preview URLs into the PR description for `feat/multigame-platform` (or save them in `.ralph/preview-urls.txt` for now if no PR yet).

**5. Flip the passes flag**

When all three previews are green and env vars are set on all three projects, edit `.ralph/prd.json` and change US-005's `"passes": false` → `"passes": true`. Then re-trigger the Ralph loop and it will advance to US-006.

### Why the agent cannot do this

- The Vercel dashboard is gated by browser auth; even `vercel project add` from CLI requires the project to first be linked to a personal/team scope, and the staging Supabase keys (US-006) live in a place the agent can't fetch programmatically.
- Per Ralph instructions ("Manual Operational Stories"), US-005 and US-006 are explicit pause points — the agent prints instructions and stops.

### Order-of-operations note

US-005 depends on US-006 (the staging Supabase env vars). Recommended order:
1. Do US-006 first (create staging Supabase project, run `bunx supabase db push --linked`, copy URL + anon key).
2. Then US-005 with those env values.
3. Flip `passes: true` for both in `.ralph/prd.json`.
4. Re-trigger Ralph; the next iteration will pick up US-007 (Phase 0 GATE).

---

## 2026-05-08 — US-006 (Phase 0.6 — Set up staging Supabase project T-5.C)
- Maintainer manually created `headball-staging` Supabase project (region: SG, ref: aulxqfmtqbwffblhxkrx).
- Maintainer ran `bunx supabase login`, `bunx supabase link --project-ref aulxqfmtqbwffblhxkrx` (with `SUPABASE_DB_PASSWORD` inline), `bunx supabase db push --linked` — applied all 12 migrations to staging.
- Maintainer created local `.env.local` with `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` (legacy JWT format — Headball's `lib/supabase.ts` and `lib/supabase-server.ts` both expect that exact var name).
- Wrote `.env.local.example` with placeholder names only (no real values), three commented sections for local/staging/prod env options.
- Added `!.env.local.example` exception to `.gitignore` so the example file IS tracked while real `.env.local` stays gitignored.
- Wrote `supabase/migrations/README.md` documenting: local-vs-staging-vs-prod env model (T-5.C), how to add a migration, additive-only discipline (T-7), realtime publication discipline (A4), rollback posture.
- Verified: `bunx tsc --noEmit` clean (no output), `.env.local.example` has no uncommented assignments (placeholders only).
- US-006 acceptance criteria all met. Flipped passes:true.
- Files changed: `.gitignore`, `.env.local.example` (new), `supabase/migrations/README.md` (new), `.ralph/prd.json`.
- **Learnings:**
  - The default Next.js `.gitignore` ignores `.env*` which also catches `.env.local.example`. Must add `!.env.local.example` exception explicitly so the example file commits while real values don't.
  - Use the LEGACY JWT anon key (eyJhbGc...) not the new `sb_publishable_*` for now — Headball's existing code was developed against the JWT format. Migrating to publishable keys is a separate v2 hygiene pass.
  - DB password belongs ONLY in: password manager + inline CLI invocations (`SUPABASE_DB_PASSWORD='...' bunx supabase ...`). Never in any `.env` file. Never in any deployed env var. Headball connects via REST/Realtime through the anon key — never needs direct Postgres password.
  - The DB password was leaked into chat history during setup — needs rotation post-setup (Settings → Database → Reset database password in Supabase Dashboard).
---

## 2026-05-08 — US-005 (Phase 0.5 — DEFERRED to Phase 4) + per-app local dev wiring
- User decision: skip Vercel project setup for now, run all 3 apps locally during Phases 0-3.
- Per-app port mapping configured:
  - Root Headball (current location, until Phase 1): http://localhost:3000
  - apps/hub: http://localhost:3001 (set via `next dev -p 3001` in package.json)
  - apps/insider: http://localhost:3002 (set via `next dev -p 3002` in package.json)
- Root package.json got 3 new convenience scripts:
  - `dev:all` → `turbo run dev --parallel` (runs all 3 in one terminal with multiplexed output)
  - `dev:hub` → just hub
  - `dev:insider` → just insider
  - `build:all` → `turbo run build` (all packages)
- Smoke test: each app responds HTTP 200 on its assigned port, Next.js 16.2.4 + Turbopack, ready in <300ms.
- US-005 marked passes:true with explicit DEFERRED note pointing to US-031 / US-037 (Phase 4 entry) where Vercel becomes mandatory for cross-subdomain redirect testing.
- Knock-on updates to early GATE stories (US-007, US-013, US-022, US-030):
  - /qa GATE requirement removed for Phases 0-3 (Vercel preview URLs don't exist yet).
  - Replaced with: typecheck + lint + vitest + playwright + manual local smoke. Sufficient for the regression risk in these phases (which are mostly mechanical relocations and additive schema changes — no Headball UI changes).
  - /qa GATE re-enabled at Phase 4 (US-037) when Vercel must be set up.
- Files changed: `apps/hub/package.json` (dev/start ports), `apps/insider/package.json` (dev/start ports), `package.json` (added dev:all + dev:hub + dev:insider + build:all), `.ralph/prd.json` (US-005 passes:true with DEFERRED note; US-007 acceptance criteria rewritten; US-013/022/030 notes added).
- **Learnings:**
  - Next.js 16 supports `next dev -p PORT` for per-app port. No need for env var (`PORT=...`) which can be brittle on Windows.
  - `turbo run dev --parallel` multiplexes all running apps' output into a single terminal — perfect for local multi-app development. `bunx turbo run dev --filter=@social-hub/hub` to run a single app standalone.
  - For Phase 1 (relocate Headball), root Headball at port 3000 will be retired and apps/headball will take over that port. Until then, root Headball stays on 3000 for backward compat with existing `bun run dev` muscle memory.
  - Visual QA via /qa needs a hosted preview URL. Until Vercel is set up (Phase 4), gate stories rely on the existing test suite + manual eyeballing. That's the right tradeoff for early-phase mechanical changes.
---

## 2026-05-08 — US-007 (Phase 0 GATE — local validation)
- Discovered & fixed a real regression Ralph triggered: pointing .env.local at staging Supabase broke 10 of 14 e2e tests. The e2e admin helper (e2e/_helpers/admin.ts) hardcodes a service-role JWT only valid for local Supabase (the demo `eyJhbGciOiJIUzI1NiI...EGIM96RAZx35lJzdJsyH-qQwv8Hdp7fsn3W0YpN81IU` token).
- Reverted .env.local to local Supabase (URL: http://127.0.0.1:54321, anon: sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH from `bunx supabase status`). Staging stays for Vercel preview env vars only.
- Killed stale dev servers on :3000/:3001/:3002 (they had cached the old staging env at boot).
- Reset local Supabase via `bunx supabase db reset`: all 12 migrations applied cleanly, 4123 football_players seeded.
- Verified all GATE criteria:
  - bunx tsc --noEmit ✓ clean
  - bun run lint ✓ clean (Ralph's earlier eslint.config.mjs change to add `**/.next/**` and `.worktrees/**` globs to flat-config ignores was correct and necessary)
  - bunx vitest run ✓ 57/57 pass
  - bunx playwright test ✓ 14/14 pass (flaky chip-suggestions test passed when full suite ran — was a transient port/server-warm-up timing issue, not a real failure)
- Wrote .ralph/qa-baseline-localhost.txt documenting that /qa baseline is deferred to Phase 4 per US-005/US-007 modified criteria.
- Files changed in this commit: .env.local (revert to local Supabase), .gitignore (Ralph's add of .ralph/.last-branch + .worktrees/), eslint.config.mjs (Ralph's add of flat-config ignore globs for **/.next, **/out, **/build, .worktrees), .ralph/qa-baseline-localhost.txt (new), .ralph/prd.json (US-007 passes:true), .ralph/progress.txt (this entry).
- Will push git tag `phase-0-done` after this commit.
- **Learnings:**
  - **CRITICAL pattern — local-vs-staging env split:** for Phases 0-3 (no Vercel previews), `.env.local` MUST point at local Supabase (`http://127.0.0.1:54321`). The e2e test infrastructure (`e2e/_helpers/admin.ts`) uses a hardcoded service-role JWT that only authenticates against the local stack. Staging needs a different service-role key set via `SUPABASE_SERVICE_ROLE_KEY` env var IF you want to run e2e against staging — but that's a v2 concern.
  - When Ralph (or any tool) edits .env.local during a phase where tests must pass, run the test suite explicitly to catch env regressions early. Lint and typecheck won't catch env mismatches.
  - `bunx supabase db reset` is the right reset between phases — recreates DB from migrations + seed. Safe because local-only.
  - The `Skipping migration README.md... (file name must match pattern)` warning from `supabase db reset` is harmless — just confirms the README we put in `supabase/migrations/` isn't being misinterpreted as a SQL migration.
  - Ralph's child claude exited mid-iteration (likely after edits to .gitignore + eslint.config.mjs but before the commit step). The bash loop's `|| true` swallowed the exit, so `git log` showed no commit. Recovery: pick up where it left off, finish manually.
---
