{
  "name": "board-game",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.1.29",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --filter=@social-hub/headball",
    "dev:all": "turbo run dev --parallel",
    "dev:hub": "turbo run dev --filter=@social-hub/hub",
    "dev:insider": "turbo run dev --filter=@social-hub/insider",
    "build": "turbo run build --filter=@social-hub/headball",
    "build:all": "turbo run build",
    "lint": "scripts/check-realtime-publication.sh && turbo run lint",
    "lint:realtime": "scripts/check-realtime-publication.sh",
    "test": "turbo run test",
    "seed:fetch": "bun run apps/headball/scripts/fetch-wikidata.ts",
    "seed:build": "bun run apps/headball/scripts/build-seed.ts",
    "seed:sql": "bun run apps/headball/scripts/seed-to-sql.ts",
    "seed:all": "bun run seed:fetch && bun run seed:build && bun run seed:sql",
    "seed:insider": "bun run scripts/seed-insider-packs.ts"
  },
  "devDependencies": {
    "@types/node": "^20",
    "supabase": "^2.95.6",
    "turbo": "^2.9.10",
    "typescript": "^5"
  },
  "ignoreScripts": [
    "sharp",
    "unrs-resolver"
  ],
  "trustedDependencies": [
    "sharp",
    "unrs-resolver"
  ]
}
