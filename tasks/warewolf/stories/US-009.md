# US-009: App scaffold + Grimoire tokens

**Lane:** B. Scaffold
**Depends on:** US-010
**Priority:** 9

## Description
As a developer, I need a working `apps/warewolf/` Next.js 16 app slot in the monorepo with all configs in place so subsequent stories can drop in code without setup overhead.

## Acceptance Criteria
- [ ] Follow the **CLAUDE.md "Adding a new app"** checklist at the repo root, line-by-line.
- [ ] `apps/warewolf/package.json`:
  - `"name": "@social-hub/warewolf"`
  - Scripts: `dev`, `build`, `lint`, `test`, `build:cards` (placeholder; filled in US-012)
  - Dependencies: `next@^16`, `react@^19`, `tailwindcss@^4`, `next-intl`, `zod`, `zustand@5.0.12`
  - DevDependencies: `vitest`, `playwright`, `@playwright/test`, `fast-check`, `sharp` (build-cards), TypeScript, `@types/*`
- [ ] `apps/warewolf/tsconfig.json` extends `../../tsconfig.base.json`. No path overrides except `@/*` → app root and `@/data/*` → workspace `data/` per CLAUDE.md path-aliases section.
- [ ] `apps/warewolf/next.config.ts` exists with minimal config.
- [ ] `apps/warewolf/vitest.config.ts` exists with `exclude: ["e2e/**"]` per CLAUDE.md (workspace-root vitest projects would otherwise collect Playwright files).
- [ ] `apps/warewolf/playwright.config.ts` exists with `workers: 1`.
- [ ] `apps/warewolf/app/globals.css` exists with Tailwind v4 `@theme inline` Grimoire tokens — mirror from US-010's `docs/DESIGN-warewolf.md` (NOT Stadium Energy). Tokens include: parchment background, ink text, blood-red `#8b1a1a`, off-cream cards, dark amber `#7a4d00`, Cormorant Garamond + Noto Serif Thai fonts, the type scale (display-xl 48, display 32, body 14 min), spacing scale, motion timing variables.
- [ ] `apps/warewolf/.gitignore` covers `.next/`, `node_modules/`, `playwright-report/`.
- [ ] `apps/warewolf/app/[lang]/layout.tsx` minimal stub (replaced fully in US-011).
- [ ] `apps/warewolf/app/[lang]/page.tsx` minimal stub returning "Warewolf placeholder" (replaced in US-019).
- [ ] `bun install` at repo root resolves the new workspace.
- [ ] `turbo run build --filter=@social-hub/warewolf` succeeds (placeholder build).
- [ ] `bunx tsc --noEmit` from `apps/warewolf/` passes.
- [ ] `bun run lint` passes workspace-wide (including the realtime publication lint, which must no-op on warewolf since there are no Supabase migrations).

## Technical Notes
- **Critical:** mirror Grimoire tokens, NOT Stadium Energy. Headball's `apps/headball/app/globals.css` is the wrong source.
- Tailwind v4 doesn't yet support cross-package `@theme` imports; tokens are copy-pasted into the app's `globals.css`. Canonical reference will live in `docs/DESIGN-warewolf.md` (US-010).
- Vercel project setup is deferred to US-027.
- Realtime publication lint script lives at workspace root; verify it exits 0 for warewolf.
