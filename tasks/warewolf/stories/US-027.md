# US-027: Vercel project + subdomain + launch

**Lane:** H. A11y + launch
**Depends on:** US-023, US-024, US-025, US-026
**Priority:** 27

## Description
As the maintainer, I need a live Vercel project + provisioned subdomain so the public Thai Werewolf community can use the tool and the share-URL flow works on real DNS.

## Acceptance Criteria
- [ ] **Vercel project created** with `rootDirectory = apps/warewolf`, framework preset = Next.js, install command = `cd ../.. && bun install`, build command = `cd ../.. && bunx turbo run build --filter=@social-hub/warewolf`.
- [ ] Per CLAUDE.md "Adding a new app" — Vercel project is **manual setup**.
- [ ] **Subdomain provisioned**: `warewolf.headball.app` (or chosen alternative per design doc line 663). DNS record (A or CNAME) configured. TLS certificate verified.
- [ ] No production env vars needed for V1 (no backend, no auth, no secrets).
- [ ] **Pushing this branch produces a successful production deploy** at the subdomain.
- [ ] **Manual smoke test** on production:
  - Three devices: iOS Safari, Android Chrome, desktop Firefox.
  - Cold load `/`, navigate to `/setup`, tap a row, customize, save URL, paste in second tab → setup reproduces.
  - Locale toggle works on every device.
- [ ] Production URL documented in `apps/warewolf/README.md`.
- [ ] **`/canary` skill run** captures pre-launch baseline against the production URL per `tasks/warewolf/prd.md` "/qa GATE PROTOCOL".
- [ ] **Git tag `warewolf-v1-launch` pushed**.
- [ ] Launch announcement drafted (NOT posted in this story — see Open Questions in PRD): screenshot of a balanced setup with vibe label + 1-paragraph Thai blurb for the 2 target Werewolf communities + 1 BGG forum thread.
- [ ] Lint passes: `bun run lint`.

## Technical Notes
- Per design doc Pass 7: `warewolf.headball.app` is the finalized hosting decision.
- Per Eng Review decision #1: card art ships as-is (Bezier Games photos). If a takedown lands post-launch, fallback to commissioned originals (see Eng Review section "Architecture decisions").
- The 4 pre-code design blockers (Pass 7) can still be open at this point — V1 ships with placeholders for the seal commission and AI-stub Thai copy; v1.1 swaps them in.
- Public posting in Thai Werewolf groups is a manual community-engagement step — DO NOT auto-post.

## Last story in Lane H — V1 COMPLETE
- [ ] After this passes, V1 is publicly live. Update `tasks/warewolf/prd.md` Success Metrics with the production URL and launch date.
- [ ] Consider scheduling `/retro` 1 week post-launch for a learnings pass.
