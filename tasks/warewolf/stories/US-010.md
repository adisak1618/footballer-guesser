# US-010: `docs/DESIGN-warewolf.md`

**Lane:** B. Scaffold
**Depends on:** none
**Priority:** 10

## Description
As a developer, I need the canonical Grimoire design system doc derived from the prototype so all subsequent UI stories share one source of truth for colors, type, spacing, components, and motion.

## Acceptance Criteria
- [ ] File created at `docs/DESIGN-warewolf.md` (NEW; sits alongside existing `docs/DESIGN.md` for Headball Stadium Energy).
- [ ] First paragraph states explicitly: **Stadium Energy from `docs/DESIGN.md` does NOT apply to Warewolf.** Warewolf is Grimoire.
- [ ] Sections (all required, per design doc Pass 5):
  - **Color palette** — parchment background, ink text, blood-red `#8b1a1a` (accent/focus/red state), off-cream `#f6ebd0` cards, dark amber `#7a4d00` (tilted balance). Hex + semantic tokens for each.
  - **Typography scale** — Cormorant Garamond Latin + Noto Serif Thai. Sizes: display-xl 48px, display 32px, h1 24px, body 14px floor (latin + thai), micro 11px floor. Italic Cormorant restricted to vibe quotes, decorative labels, role names in titles, dropcaps. Body upright per Pass 6.
  - **Spacing scale** — extract from prototype CSS variables.
  - **Component vocabulary** — button, card, modal/sheet, banner, balance-scale, tile, ornament-frame, role-detail-block. One paragraph each describing intent + key props.
  - **Iconography rules** — wax seals (Unicode placeholder V1; bespoke SVG v1.1 commission), fleurons (`*` separator), ornament dots.
  - **Motion language** — full timing table per Pass 7:
    - Balance pointer 200ms ease-out
    - Modal/sheet 280ms `cubic-bezier(0.16, 1, 0.3, 1)`
    - Tile press 150ms ease-out
    - Banner state fade 200ms ease-in-out
    - Page transitions instant
    - All durations → 0ms when `prefers-reduced-motion: reduce`.
- [ ] **Token names match those used in `apps/warewolf/app/globals.css`** (US-009) — write the CSS variables here first, then US-009 mirrors them.
- [ ] Cross-reference: link to the prototype `~/.gstack/projects/board-game/designs/warewolf-full-app-20260512/finalized.html` and the 5 approved wireframes.
- [ ] Lint passes: `bun run lint` (markdown lint if configured; otherwise N/A).

## Technical Notes
- Extract the source-of-truth CSS variables from the prototype's `<style>` block. The prototype is the visual ground truth.
- This is a documentation story, not code. No typecheck applies.
- US-009 depends on this; US-013, US-014, US-015, US-016, US-017, US-019, US-020, US-021, US-022 all reference it.

## Last story in Lane B
- [ ] After this and US-009 pass, push a WIP commit `feat: warewolf lane B complete (scaffold + design system)`.
