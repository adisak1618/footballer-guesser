# US-003: Archetypes — `apps/warewolf/lib/archetypes.ts`

**Lane:** A. Data
**Depends on:** US-001
**Priority:** 3

## Description
As a developer, I need the 8 archetype objects with player-count caps and i18n display names so the setup list and chip strip can gate which archetypes are valid for a given player count.

## Acceptance Criteria
- [ ] File created at `apps/warewolf/lib/archetypes.ts`.
- [ ] Exports `ARCHETYPES: Record<ArchetypeId, Archetype>` with 8 entries.
- [ ] Each archetype has: `id`, `seal` (Unicode glyph placeholder for V1; SVG commission deferred per Pass 4 blocker #1), `minPlayers`, `maxPlayers`, `wolfDelta: 0 | 1`, `i18n: { en, th }` with `name` and `vibe`.
- [ ] Final caps per design doc lines 574–582 (and Reconciliation pass):
  - `classic-detective: 6–20, wolfDelta 0`
  - `wolf-chaos: 5–9, wolfDelta 1` (the only archetype with +1 wolf)
  - `info-heavy: 7–20, wolfDelta 0`
  - `beginner: 6–13, wolfDelta 0`
  - `power-roles: 6–20, wolfDelta 0`
  - `social-bluff: 5–13, wolfDelta 0`
  - `neutral-mayhem: 7–13, wolfDelta 0`
  - `balanced-power: 7–20, wolfDelta 0`
- [ ] Exports `ArchetypeId` as a string-literal union of the 8 ids.
- [ ] Tests written: `apps/warewolf/lib/archetypes.test.ts` asserting (a) 8 entries, (b) `minPlayers ≤ maxPlayers` for each, (c) every entry has en + th name + vibe, (d) only `wolf-chaos` has `wolfDelta === 1`.
- [ ] Typecheck passes: `cd apps/warewolf && bunx tsc --noEmit`.
- [ ] Lint passes: `bun run lint`.

## Technical Notes
- Seal glyphs are placeholders (Unicode characters from prototype). Production launch may swap for bespoke SVGs (Pass 4 blocker).
- Display names are pinned IDs; Thai display names may need a native-speaker revision (Pass 7 blocker #3 + long-tail).
- Data model in design doc lines 358–368.
