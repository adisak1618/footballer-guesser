# US-008: Category tabs — `apps/warewolf/lib/category-tabs.ts`

**Lane:** A. Data
**Depends on:** US-001
**Priority:** 8

## Description
As a developer, I need a single source of truth for which UI tab each role belongs to in the Add Role sheet, so the 6 tabs (Wolves/Info/Power/Vanilla/Social/Neutral) cleanly partition the 22 roles with no orphans.

## Acceptance Criteria
- [ ] File created at `apps/warewolf/lib/category-tabs.ts`.
- [ ] Exports `TabKey = 'wolves' | 'info' | 'power' | 'vanilla' | 'social' | 'neutral'`.
- [ ] Exports `mapCategoryToTab(role: Role): TabKey` per locked mapping in Eng Review decision #6:
  - `wolves` ← `team === 'werewolf'`
  - `info` ← `category === 'info'`
  - `power` ← `category ∈ {'protection', 'kill', 'vote'}`
  - `vanilla` ← `category === 'vanilla'` AND NOT in social subset
  - `social` ← `category ∈ {'chaos', 'vanilla-social'}` (where vanilla-social is the subset of vanilla roles like `mason`, `old-hag`, `spellcaster` per design doc Premise #8)
  - `neutral` ← `team === 'neutral'`
- [ ] JSDoc on `mapCategoryToTab` spells the full mapping table verbatim. The mapping is part of the public contract.
- [ ] Exports `TABS: readonly TabKey[]` in display order for the sheet.
- [ ] Exports `rolesForTab(tab: TabKey, roles: Role[] = Object.values(ROLES)): Role[]` helper.
- [ ] Tests written: `apps/warewolf/lib/category-tabs.test.ts`:
  - **Every role belongs to exactly one tab — no orphans, no duplicates.** Iterate all 22 V1 roles.
  - `wolves` tab returns 5 wolf-team roles.
  - `neutral` tab returns 4 neutral-team roles.
  - All 22 roles accounted for across the 6 tabs.
- [ ] Typecheck passes: `cd apps/warewolf && bunx tsc --noEmit`.
- [ ] Lint passes: `bun run lint`.

## Technical Notes
- Per Eng Review decision #6: derive from team + category, no new field on Role.
- Defining the social subset: per design doc Premise #8, village vanilla/social roles are `villager, mason, spellcaster, old-hag`. Of these, `mason`, `spellcaster`, `old-hag` go to `social`; pure `villager` goes to `vanilla`. Encode this as a hard list in `category-tabs.ts` or via a derived rule.
- Used by US-017 (RoleDetailModal + AddRoleSheet).

## Last story in Lane A
- [ ] After this passes, push a WIP commit `feat: warewolf lane A complete (US-001..US-008)`.
