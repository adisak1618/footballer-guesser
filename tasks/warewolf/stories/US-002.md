# US-002: Wolf pool constants — `apps/warewolf/lib/wolf-pools.ts`

**Lane:** A. Data
**Depends on:** US-001
**Priority:** 2

## Description
As a developer, I need the per-archetype wolf allow-list and the wolf-composition constraints so the solver searches only valid wolf mixes per archetype identity.

## Acceptance Criteria
- [ ] File created at `apps/warewolf/lib/wolf-pools.ts`.
- [ ] Exports `WOLF_POOLS: Record<ArchetypeId, RoleId[]>` with 8 entries per design doc lines 376–385:
  - `classic-detective: ['werewolf','wolf-cub','sorceress','alpha-wolf']`
  - `wolf-chaos: ['wolf-cub','werewolf','minion','sorceress','alpha-wolf']`
  - `info-heavy: ['werewolf','wolf-cub','sorceress']`
  - `beginner: ['werewolf','wolf-cub']`
  - `power-roles: ['wolf-cub','werewolf','alpha-wolf','sorceress']`
  - `social-bluff: ['werewolf','wolf-cub','minion','sorceress']`
  - `neutral-mayhem: ['werewolf','wolf-cub','sorceress']`
  - `balanced-power: ['wolf-cub','werewolf','alpha-wolf','sorceress']`
- [ ] Exports `SINGLETON_WOLVES: ReadonlySet<RoleId>` = `{alpha-wolf, minion, sorceress}` (max 1 each).
- [ ] Exports `PACK_REQUIRED: ReadonlySet<RoleId>` = `{wolf-cub, alpha-wolf, minion}` (excluded when `wolfCount === 1`).
- [ ] Exports `MAX_WOLF_CUB = 2`.
- [ ] Three explanatory comments ported verbatim from prototype `finalized.html:683–690` (per Eng Review code-quality fix). They explain the *why* of each constraint.
- [ ] Tests written: `apps/warewolf/lib/wolf-pools.test.ts` asserting (a) all 8 archetype keys present, (b) every role id in pools exists in `ROLES`, (c) SINGLETON_WOLVES ⊆ union of all pools.
- [ ] Typecheck passes: `cd apps/warewolf && bunx tsc --noEmit`.
- [ ] Lint passes: `bun run lint`.

## Technical Notes
- Match prototype `finalized.html:683–690` for the three WHY comments — they're load-bearing.
- These constants are imported by `lib/solver.ts` (US-005). No UI code depends on them directly.
